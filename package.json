{
  "name": "gradescope-api-ts",
  "version": "1.0.0",
  "description": "A TypeScript library for interacting with Gradescope (via web scraping)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "npm run build && node dist/example.js",
    "start:dev": "ts-node src/example.ts",
    "list-courses": "npm run build && node dist/list-courses.js",
    "list-courses:dev": "ts-node src/list-courses.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["gradescope", "api", "scraper", "typescript"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.8",
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.4.5",
    "luxon": "^3.4.4"
  },
  "devDependencies": {
    "@types/luxon": "^3.4.2",
    "@types/node": "^20.11.30",
    "@types/cheerio": "^0.22.35",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
